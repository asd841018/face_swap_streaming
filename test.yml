# 啟用 API (預設 Port 9997)
api: yes

# 啟用 WebRTC 伺服器
webrtc: yes
webrtcLocalTCPAddress: :8189
webrtcICEServers2:
  - url: stun:stun.l.google.com:19302
# ------------------------------------------------

# (可選) 保留您之前的 HLS 優化設定
# hlsSegmentDuration: 1s
# hlsSegmentCount: 10
# hlsAllowOrigin: '*'

paths:
  all:
    # # # 當有串流開始發布時，呼叫 on_publish
    # runOnReady: curl -X POST -d "path=$MTX_PATH" -d "query=$MTX_QUERY" http://localhost:7980/on_publish
    
    # # # 當串流結束時，呼叫 on_publish_done
    # runOnNotReady: curl -X POST -d "path=$MTX_PATH" http://localhost:7980/on_publish_done